Source code for the spassberlin.de website. Built with NextJs.

### Development

```
yarn install
yarn dev
```

### Deployment

Pushing to `main` will trigger GitHub Actions and if build is successful, the `out` directory will be pushed to `gh-pages` branch.